<nav class="navbar navbar-expand-lg bg-body-tertiary fixed-top">
  <div class="container-fluid">
    <div style="display: flex;">
      <% if user_signed_in? %>
      <div>
        <%= image_tag "logo.png"%>
        <%= link_to 'Dashboard', dashboard_path, style: "text-decoration: none", class: "mx-2" %>
          <!-- Add more links for authenticated users -->
      </div>
      <% else %>
        <!-- Add links for non-authenticated users (e.g., sign-up, sign-in) -->
      <% end %>
    </div>
  </div>
</nav>